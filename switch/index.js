

// Switch statement
var firstName = 'John';
var job = 'instructor';

switch (job) {
    case 'instructor':
        document.write(firstName + ' teaches kids how to code.');
        break;
    case 'driver':
        document.write(firstName + ' drives an uber in Lisbon.');
        break;
    case 'designer':
        document.writeg(firstName + ' designs beautiful websites.');
        break;
    default:
        document.write(firstName + ' does something else.');
}